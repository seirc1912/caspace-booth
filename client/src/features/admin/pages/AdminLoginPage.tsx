import { useState } from 'react'
import { BrandMark } from '../../../components/branding/BrandMark'

export function AdminLoginPage({ onAuthenticated }: { onAuthenticated: () => void }) {
  const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(false)
  const submit = () => { const valid = /^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email) && password.length >= 8; setError(!valid); if (valid) { sessionStorage.setItem('selfbooth.admin-session.v1', JSON.stringify({ email, authenticatedAt: new Date().toISOString() })); onAuthenticated() } }
  return <main className="grid min-h-dvh place-items-center bg-[#f4f3f1] p-5"><section className="w-full max-w-md rounded-[2rem] bg-white p-7 shadow-xl shadow-stone-900/5"><BrandMark /><p className="mt-8 text-sm font-bold uppercase tracking-[.2em] text-violet-600">Protected workspace</p><h1 className="mt-2 text-3xl font-black">Admin sign in</h1><p className="mt-2 text-sm leading-6 text-stone-500">Use your administrator account to open Template Studio.</p><div className="mt-7 grid gap-4"><label className="text-sm font-bold">Email<input autoComplete="username" className="mt-2 min-h-12 w-full rounded-xl border border-stone-200 px-4 font-normal" onChange={(event) => setEmail(event.target.value)} type="email" value={email} /></label><label className="text-sm font-bold">Password<input autoComplete="current-password" className="mt-2 min-h-12 w-full rounded-xl border border-stone-200 px-4 font-normal" minLength={8} onChange={(event) => setPassword(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter') submit() }} type="password" value={password} /></label>{error ? <p className="rounded-xl bg-rose-50 p-3 text-sm font-semibold text-rose-700" role="alert">Enter a valid email and a password of at least 8 characters.</p> : null}<button className="min-h-14 rounded-2xl bg-stone-950 px-5 font-bold text-white" onClick={submit} type="button">Sign in</button></div><p className="mt-5 text-xs leading-5 text-stone-400">Frontend session gate for this backend-free build. Production identity must be verified by the future admin API.</p></section></main>
}
