import { useMemo, useState } from 'react'
import { usePathname } from '../../../hooks/usePathname'
import { useAdminTemplates } from '../store/AdminTemplateContext'
import { useRooms } from '../store/RoomContext'
import type { TemplateStatus } from '../types'

const updatedTime = new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' })

export function AdminTemplatesPage() {
  const store = useAdminTemplates()
  const { rooms } = useRooms()
  const { navigate } = usePathname()
  const [query, setQuery] = useState('')
  const [status, setStatus] = useState<'all' | TemplateStatus>('all')
  const [roomId, setRoomId] = useState('all')
  const [sort, setSort] = useState<'updated' | 'name'>('updated')
  const records = useMemo(() => store.templates.filter((record) => record.template.name.toLowerCase().includes(query.toLowerCase()) && (status === 'all' || record.status === status) && (roomId === 'all' || record.roomId === roomId)).sort((a, b) => sort === 'name' ? a.template.name.localeCompare(b.template.name) : b.updatedAt.localeCompare(a.updatedAt)), [query, roomId, sort, status, store.templates])
  const rename = (id: string) => { const source = store.templates.find((item) => item.id === id); const name = source && window.prompt('Template name', source.template.name)?.trim(); if (source && name) store.save({ ...source, updatedAt: new Date().toISOString(), template: { ...source.template, name } }) }
  const assignRoom = (id: string, nextRoomId: string) => { const source = store.templates.find((item) => item.id === id); if (source) store.save({ ...source, roomId: nextRoomId, updatedAt: new Date().toISOString() }) }

  return <div><header className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-3xl font-bold tracking-tight">Templates</h1><p className="mt-1 text-stone-500">Create, organize, publish, and maintain print layouts.</p></div><button className="min-h-12 rounded-xl bg-stone-950 px-5 font-semibold text-white" onClick={() => navigate('/admin/editor')} type="button">New template</button></header><div className="mt-6 grid gap-2 rounded-2xl bg-white p-3 shadow-sm sm:grid-cols-4"><input aria-label="Search templates" className="min-h-11 rounded-xl border border-stone-200 px-3 text-sm" onChange={(event) => setQuery(event.target.value)} placeholder="Search templates…" value={query} /><select aria-label="Filter by status" className="min-h-11 rounded-xl border border-stone-200 px-3 text-sm" onChange={(event) => setStatus(event.target.value as typeof status)} value={status}><option value="all">All statuses</option><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select><select aria-label="Filter by room" className="min-h-11 rounded-xl border border-stone-200 px-3 text-sm" onChange={(event) => setRoomId(event.target.value)} value={roomId}><option value="all">All rooms</option>{rooms.map((room) => <option key={room.id} value={room.id}>{room.name}</option>)}</select><select aria-label="Sort templates" className="min-h-11 rounded-xl border border-stone-200 px-3 text-sm" onChange={(event) => setSort(event.target.value as typeof sort)} value={sort}><option value="updated">Recently updated</option><option value="name">Name A–Z</option></select></div>{records.length === 0 ? <div className="mt-7 rounded-2xl border border-dashed border-stone-300 p-12 text-center text-stone-500">No templates match these filters.</div> : <div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{records.map((record) => <article className="overflow-hidden rounded-2xl bg-white shadow-sm" key={record.id}><div className="grid aspect-[16/9] place-items-center bg-stone-100">{record.coverUrl || record.template.thumbnailUrl ? <img alt="" className="h-full w-full object-cover" src={record.coverUrl ?? record.template.thumbnailUrl ?? ''} /> : <span className="text-sm text-stone-400">No cover</span>}</div><div className="p-4"><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold">{record.template.name}</h2><p className="mt-1 text-xs text-stone-500">{record.info.category} · {record.template.slotCount} slots</p><p className="mt-1 text-[11px] text-stone-400">Updated {updatedTime.format(new Date(record.updatedAt))}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold uppercase ${record.status === 'published' ? 'bg-emerald-100 text-emerald-700' : record.status === 'archived' ? 'bg-stone-200 text-stone-500' : 'bg-amber-100 text-amber-700'}`}>{record.status}</span></div><select aria-label={`Room for ${record.template.name}`} className="mt-3 min-h-10 w-full rounded-lg border border-stone-200 px-2 text-xs" onChange={(event) => assignRoom(record.id, event.target.value)} value={record.roomId}>{rooms.map((room) => <option key={room.id} value={room.id}>{room.name}</option>)}</select><div className="mt-3 grid grid-cols-2 gap-2"><button className="min-h-10 rounded-lg bg-stone-950 text-sm font-semibold text-white" onClick={() => navigate(`/admin/templates/${record.id}`)} type="button">Edit</button><button className="min-h-10 rounded-lg bg-stone-100 text-sm font-semibold" onClick={() => rename(record.id)} type="button">Rename</button><button className="min-h-10 rounded-lg bg-stone-100 text-sm font-semibold" onClick={() => store.duplicate(record.id)} type="button">Duplicate</button><button className="min-h-10 rounded-lg bg-stone-100 text-sm font-semibold" onClick={() => store.setStatus(record.id, record.status === 'published' ? 'draft' : 'published')} type="button">{record.status === 'published' ? 'Unpublish' : 'Publish'}</button><button className="min-h-10 rounded-lg bg-stone-100 text-sm font-semibold" onClick={() => store.setStatus(record.id, 'archived')} type="button">Archive</button><button className="min-h-10 rounded-lg text-sm font-semibold text-rose-600 hover:bg-rose-50" onClick={() => { if (window.confirm(`Delete ${record.template.name}?`)) store.remove(record.id) }} type="button">Delete</button></div></div></article>)}</div>}</div>
}
